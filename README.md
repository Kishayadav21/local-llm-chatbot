# local-llm-chatbot
A Streamlit-based local AI chatbot that runs completely offline using Ollama models. No API key required. Supports multiple LLMs with adjustable temperature and response length controls.
